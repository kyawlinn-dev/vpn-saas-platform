[defaults]
inventory = inventory.ini
host_key_checking = False
remote_user = root
# Uncomment to avoid typing the vault password on every run once
# group_vars/novanet/vault.yml exists. Keep the referenced file out of git
# (see .gitignore) — it is the vault decryption password, not a secret to
# commit.
# vault_password_file = .vault_pass
